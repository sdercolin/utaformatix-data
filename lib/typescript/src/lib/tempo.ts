/**
 * Tempo label model.
 * @param tickPosition - Int64. Tick position of the tempo label.
 * @param bpm - Double. Tempo in beats-per-minute.
 */
export type Tempo = {
  tickPosition: number;
  bpm: number;
};
